export type FamilyContact={id:string;displayName:string;role:'admin'|'member';status:string};

type Props={members:FamilyContact[];currentMemberId:string;selectedMemberId?:string|null;onSelectMember?:(member:FamilyContact|null)=>void};

export function FamilyContacts({members,currentMemberId,selectedMemberId=null,onSelectMember}:Props){
  const active=members.filter(member=>member.status==='active');
  return <section className="panel contacts-panel"><h2>Контакты</h2><div className="contacts-list">{onSelectMember&&<button type="button" className="contact-row contact-button" data-chat="family" aria-current={selectedMemberId===null?'page':undefined} onClick={()=>onSelectMember(null)}><div className="contact-avatar" aria-hidden="true">⌂</div><div className="contact-copy"><strong>Общий чат</strong><span>Вся семья</span></div></button>}{active.map(member=>member.id===currentMemberId?<div className="contact-row" key={member.id}><div className="contact-avatar" aria-hidden="true">{member.displayName.trim().slice(0,1).toUpperCase()||'•'}</div><div className="contact-copy"><strong>{member.displayName}</strong><span>Вы</span></div></div>:<button type="button" className="contact-row contact-button" data-member-id={member.id} aria-current={selectedMemberId===member.id?'page':undefined} key={member.id} onClick={()=>onSelectMember?.(member)}><div className="contact-avatar" aria-hidden="true">{member.displayName.trim().slice(0,1).toUpperCase()||'•'}</div><div className="contact-copy"><strong>{member.displayName}</strong><span>{member.role==='admin'?'Администратор':'Член семьи'}</span></div></button>)}</div></section>;
}
