name: Android APK

on:
  push:
    branches:
      - feat/phase3c7-android-apk-pwa-release
  pull_request:
    branches:
      - feat/phase3c6-e2ee-attachments

jobs:
  build-apk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Build debug APK
        run: gradle -p apps/android-shell :app:assembleDebug --no-daemon
      - name: Prepare artifact
        run: |
          mkdir -p artifacts
          cp apps/android-shell/app/build/outputs/apk/debug/app-debug.apk artifacts/FamilyMessenger-debug.apk
      - uses: actions/upload-artifact@v4
        with:
          name: FamilyMessenger-debug-apk
          path: artifacts/FamilyMessenger-debug.apk
          if-no-files-found: error
